import { connect } from 'react-redux';
import PostForm from './post_form';

const msp = state => {
    return {

    };
};

const mdtp = dispatch => {
    return {

    };
};

export default connect(
    msp, 
    mdtp
)(PostForm)